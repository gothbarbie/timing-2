const initialState = [
  {
    name: 'tag',
    count: 1,
  },
]

const tagsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default tagsReducer
